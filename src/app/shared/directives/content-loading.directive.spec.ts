import { ContentLoadingDirective } from './content-loading.directive';

describe('ContentLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
